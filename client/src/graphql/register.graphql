mutation Register ($email: String!, $password: String!, $lastname: String!, $firstname: String!) {
    register(
        data: {
            firstname: $firstname,
            lastname: $lastname,
            email: $email,
            password: $password
        }
    ) {
        id
        lastname
        firstname
        name
        email
    }

}